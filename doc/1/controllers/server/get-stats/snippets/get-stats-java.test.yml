name: java-server#getStats
description: Returns statistics snapshots within a provided timestamp range.
hooks:
  before:
  after:
template: print-result
expected: hits
protocols:
  - http
  - websocket
