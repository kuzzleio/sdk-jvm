name: java-server#now
description: Fetch the current server timestamp, in Epoch-millis format.
hooks:
  before:
  after:
template: print-result
exptected: (([0-1]?[0-9])|(2[0-3])):[0-5][0-9]:[0-5][0-9]protocols:
  - http
  - websocket
